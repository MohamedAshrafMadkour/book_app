import 'package:flutter/material.dart';

class TestDataNavigation extends StatelessWidget {
  const TestDataNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
